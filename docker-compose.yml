services:
  app:
    build: .
    ports:
      - "5173:5173"
      - "4000:4000"
    environment:
      PORT: 4000
      VITE_SOCKET_URL: http://localhost:4000
    command: sh -c "npm install && npm run build && npx tsx server/index.ts"
